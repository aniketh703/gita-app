/**
 * Explore Tab Screen
 * Explore and discover content - placeholder without styling
 */

import React from 'react';
import { View, Text } from 'react-native';
import type { ExploreTabScreenProps } from '@/src/types/navigation';

export default function ExploreScreen({ navigation, route }: ExploreTabScreenProps) {
  return (
    <View>
      <Text>Explore Tab</Text>
    </View>
  );
}
